二叉搜索树的特点：左小右大——对于树中每个节点，其左子树的每个节点的值都要小于这个节点的值，右子树的每个节点的值都要大于这个节点的值。

左小右大的特性能够帮助我们在 BST 中快速找到某个节点，或者找到在某个范围内的所有节点。

在查找目标元素的场景中，BST 树理想的时间复杂度是树的高度 $O(\log N)$，而普通二叉树的的遍历函数需要 $O(N)$ 的时间。

至于其余的增删改操作，也要先定位到目标节点，然后修改指针，所以增删改的时间复杂度也是 $O(\log N)$。

