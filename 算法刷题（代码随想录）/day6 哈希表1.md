# [有效的字母异位词](https://leetcode.cn/problems/valid-anagram/)

> [!question]
> 给定两个字符串 `s` 和 `t` ，编写一个函数来判断 `t` 是否是 `s` 的字母异位词。
> 
> **示例 1:**
> 
> **输入:** s = "anagram", t = "nagaram"
> **输出:** true
> 
> **示例 2:**
> 
> **输入:** s = "rat", t = "car"
> **输出:** false
> 
> **提示:**
> 
> - `1 <= s.length, t.length <= 5 * 104`
> - `s` 和 `t` 仅包含小写字母
> 
> **进阶:** 如果输入字符串包含 unicode 字符怎么办？你能否调整你的解法来应对这种情况？

```cpp
bool isAnagram(string s, string t) {
  vector<int> count1 = encode(s);
  vector<int> count2 = encode(t);
  for (int i = 0; i < count1.size(); i++) {
    if (count1[i] != count2[i]) {
      return false;
    }
  }
  return true;
}
vector<int> encode(string s) {
  vector<int> count(26, 0);
  for (char c : s) {
    int delta = c - 'a';
    count[delta]++;
  }
  return count;
}
```

```cpp
class Solution {
public:
    bool isAnagram(string s, string t) {
        // 如果长度都不等，肯定不是异位词
        if (s.length() != t.length()) return false;

        // 使用固定大小的数组（栈分配），比 vector 更快
        int record[26] = {0};

        // 一个加，一个减
        for (int i = 0; i < s.length(); i++) {
            record[s[i] - 'a']++;
            record[t[i] - 'a']--;
        }

        // 检查是否有任何位置不为 0
        for (int i = 0; i < 26; i++) {
            if (record[i] != 0) return false;
        }

        return true;
    }
};
```

方法二比起方法一：空间减半，逻辑合一

我们可以只使用**一个数组**。
- 遍历第一个字符串时，在对应位置 **加 1**。
- 遍历第二个字符串时，在对应位置 **减 1**。
- 最后检查数组，如果所有位置都回到了 **0**，说明它们完全抵消，就是异位词。

## 如果字符范围变大（不仅是 'a'-'z'）？

如果题目说字符串包含 Unicode 字符（比如中文或特殊符号），我们就不能用 `int[26]` 了，这时候可以回退到哈希表：

```cpp
class Solution {
public:
    bool isAnagram(string s, string t) {
        if (s.length() != t.length()) return false;
        unordered_map<char, int> counts;
        for (char c : s) counts[c]++;
        for (char c : t) {
            counts[c]--;
            if (counts[c] < 0) return false; // 优化：只要减到负数，说明 t 多出了字符
        }
        return true;
    }
};
```

# [快乐数](https://leetcode.cn/problems/happy-number/)

> [!question]
> 编写一个算法来判断一个数 `n` 是不是快乐数。
> 
> **「快乐数」** 定义为：
> 
> - 对于一个正整数，每一次将该数替换为它每个位置上的数字的平方和。
> - 然后重复这个过程直到这个数变为 1，也可能是 **无限循环** 但始终变不到 1。
> - 如果这个过程 **结果为** 1，那么这个数就是快乐数。
> 
> 如果 `n` 是 _快乐数_ 就返回 `true` ；不是，则返回 `false` 。
> 
> **示例 1：**
> 
> **输入：** n = 19
> **输出：** true
> **解释：**
> 12 + 92 = 82
> 82 + 22 = 68
> 62 + 82 = 100
> 12 + 02 + 02 = 1
> 
> **示例 2：**
> 
> **输入：** n = 2
> **输出：** false
> 
> **提示：**
> 
> - `1 <= n <= 231 - 1`

## 1. 快乐数的逻辑本质

题目定义：对于一个正整数，每一次将该数替换为它每个位置上的数字的平方和。

- 如果最终结果变成了 **1**，那它就是快乐数。
- 如果进入了 **无限循环** 且始终变不到 1，那它就不是快乐数。

## 2. 核心挑战：如何判断“无限循环”？

既然我们要判断是否进入了死循环，也就是判断 **“某个数字是否曾经出现过”**。在 C++ 算法中，这有两种标准的解法：

### 方案 A：哈希表（`unordered_set`）

这是最直观的方法。我们每计算出一个平方和，就把它存进集合里。

- 如果新算的数已经在集合里了，说明**撞车了（进入循环）**，直接判定不快乐。
- 如果变成了 1，判定快乐。

```C++
#include <unordered_set>

class Solution {
public:
    // 提取各位平方和的辅助函数
    int getNext(int n) {
        int sum = 0;
        while (n > 0) {
            int d = n % 10;
            sum += d * d;
            n /= 10;
        }
        return sum;
    }

    bool isHappy(int n) {
        std::unordered_set<int> seen;
        // 只要 n 没变成 1 且 没在集合里出现过，就继续算
        while (n != 1 && !seen.count(n)) {
            seen.insert(n);
            n = getNext(n);
        }
        return n == 1;
    }
};
```

### 方案 B：快慢指针（Floyd's Cycle-Finding Algorithm）

这才是这道题最“高级”的解法！既然是判断循环，**这和判断链表是否有环（LeetCode 141）逻辑完全一样**。

- **慢指针 (slow)**：每次计算一次平方和。
- **快指针 (fast)**：每次连续计算两次平方和。
- **结论**：如果存在循环，快慢指针一定会相遇。如果相遇时数字不是 1，就不是快乐数。

```cpp
class Solution {
public:
    int getNext(int n) {
        int sum = 0;
        while (n > 0) {
            int d = n % 10;
            sum += d * d;
            n /= 10;
        }
        return sum;
    }

    bool isHappy(int n) {
	    int slow = n, fast = n;
	    do {
	        slow = getNext(slow);         // 走 1 步
	        fast = getNext(getNext(fast)); // 走 2 步
	    } while (slow != fast);           // 只要没相遇，就一直跑
	
	    // 撞上了！看看撞上的地方是不是 1
	    return slow == 1;
	}
};
```

## 为什么快慢指针更好 ？

1. **空间复杂度低**：哈希表需要 $O(n)$ 的空间来存储路径，而快慢指针只需要 $O(1)$ 的常数空间。
2. **代码优雅**：不需要引入额外的容器。

从 **链表是否有环** 到 **快乐数**，算法底层逻辑的迁移：
- 链表：指针的指向形成了路径。
- 快乐数：**数字的变换规则** 形成了路径。

# [两数之和](https://leetcode.cn/problems/two-sum/)

> [!question]
> 给定一个整数数组 `nums` 和一个整数目标值 `target`，请你在该数组中找出 **和为目标值** _ `target` _  的那 **两个** 整数，并返回它们的数组下标。
> 你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。
> 
> 你可以按任意顺序返回答案。
> 
> **示例 1：**
> 
> **输入：** nums = [2,7,11,15], target = 9
> **输出：** [0,1]
> **解释：** 因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
> 
> **示例 2：**
> 
> **输入：** nums = [3,2,4], target = 6
> **输出：** [1,2]
> 
> **示例 3：**
> 
> **输入：** nums = [3,3], target = 6
> **输出：** [0,1]
> 
> **提示：**
> 
> - `2 <= nums.length <= 104`
> - `-109 <= nums[i] <= 109`
> - `-109 <= target <= 109`
> - **只会存在一个有效答案**

### 1. 暴力解法 (Brute Force)

最直白的方法是使用两层循环，枚举所有的两两组合，看它们的和是否等于目标值。

- **思路**：外层循环固定一个数，内层循环去寻找另一个数。
- **复杂度**：时间 $O(n^2)$，空间 $O(1)$。

```C++
vector<int> twoSum(vector<int>& nums, int target) {
    for (int i = 0; i < nums.size(); i++) {
        for (int j = i + 1; j < nums.size(); j++) {
            if (nums[i] + nums[j] == target) {
                return {i, j};
            }
        }
    }
    return {};
}
```

### 2. 哈希表优化 (Hash Map) —— 推荐做法

暴力法的瓶颈在于：为了找“另一个数”，我们又开了一层循环。如果我们能“记住”之前看过的数字，就能把寻找的时间降到 $O(1)$。

- **核心思想**：当我们遍历到数字 `x` 时，我们其实是在找 `target - x` 是否在之前出现过。
- **工具**：C++ 中的 `std::unordered_map`。
- **复杂度**：时间 $O(n)$，空间 $O(n)$。

```C++
#include <unordered_map>

vector<int> twoSum(vector<int>& nums, int target) {
    unordered_map<int, int> hashtable;
    for (int i = 0; i < nums.size(); i++) {
        // 计算我们需要的那个“另一半”
        int complement = target - nums[i];
        
        // 看看“另一半”是否在哈希表里
        if (hashtable.find(complement) != hashtable.end()) {
            // 找到了！返回“另一半”的下标和当前下标
            return {hashtable[complement], i};
        }
        
        // 没找到，就把当前的数字和下标存入哈希表
        hashtable[nums[i]] = i;
    }
    return {};
}
```

### 如果是“有序数组”？

如果题目想找的数组是已经排好序（当然，我们也可以自己 sort 一下）的，那么可以使用更节省空间的双指针（Two Pointers）法：

- 左指针指向头，右指针指向尾。
- 如果和太大，右指针左移；如果和太小，左指针右移。
- **复杂度**：时间 $O(n)$，空间 $O(1)$。

### 💡 总结

- **普通数组**：用哈希表，核心是“空间换时间”。
- **有序数组**：用双指针，核心是“利用顺序缩减搜索空间”。

# [两个数组的交集](https://leetcode.cn/problems/intersection-of-two-arrays/)

> [!question]
> 给定两个数组 `nums1` 和 `nums2` ，返回它们的交集。输出结果中的每个元素一定是 **唯一** 的。我们可以 **不考虑输出结果的顺序** 。
> 
> **示例 1：**
> 
> **输入：** nums1 = [1,2,2,1], nums2 = [2,2]
> **输出：** [2]
> 
> **示例 2：**
> 
> **输入：** nums1 = [4,9,5], nums2 = [9,4,9,8,4]
> **输出：** [9,4]
> **解释：** [4,9] 也是可通过的
> 
> **提示：**
> 
> - `1 <= nums1.length, nums2.length <= 1000`
> - `0 <= nums1[i], nums2[i] <= 1000`
> 
> 

利用集合元素的唯一性。

```cpp
vector<int> intersection(vector<int> &nums1, vector<int> &nums2) {
    unordered_set<int> res;
    unordered_set<int> nums_set(nums1.begin(), nums1.end());
    for (int num : nums2) {
      if (nums_set.find(num) != nums_set.end()) {
        res.insert(num);
      }
    }
    return vector<int>(res.begin(), res.end());
  }
```
